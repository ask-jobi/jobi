---
description: 
globs: components/**/*
alwaysApply: false
---
---
description: Defines the file structure and naming conventions for React components.
globs: components/**/*
---
- 页面组件放在 app/，通用组件放在 components/ui/，业务组件放在 components/client-components/。
- 组件目录命名统一用小写短横线（如 resume-templates），文件名与导出组件名保持一致。
- 组件内部如有 hooks、types、utils，如果可以复用，建议放在 lib/hooks/、types/、lib/utils.ts中。
- 推荐使用 TypeScript 接口定义 props，禁止使用 any。
- 组件优先使用命名导出，避免默认导出。
- 组件应为纯函数组件，禁止类组件。
- 样式统一用 Tailwind CSS，禁止内联 style。
- 组件应有必要的单元测试（如 jest + @testing-library/react）。